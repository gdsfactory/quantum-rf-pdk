name: Model Regression
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
permissions: {}
jobs:
  model-regression:
    permissions:
      contents: write
      pull-requests: write
    uses: doplaydo/pdk-ci-workflow/.github/workflows/model_regression.yml@ae9c40561a365ac980256651b03a34c604eaa904 # main
    secrets: inherit # zizmor: ignore[secrets-inherit]
