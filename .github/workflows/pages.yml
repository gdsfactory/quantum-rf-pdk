name: Docs
on:
  pull_request:
  push:
    branches:
      - main
  merge_group:
  workflow_dispatch:
jobs:
  build-docs:
    runs-on: ubuntu-latest
    name: Build HTML docs
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v6
      - run: make install
      - run: make docs
      - uses: actions/upload-pages-artifact@v3
        with:
          path: "./docs/_build/html/"
  build-docs-pdf:
    runs-on: ubuntu-latest
    name: Build PDF docs
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v6
      - run: make install
      - run: make docs-latex || echo "LaTeX build completed with warnings, continuing…"
      - name: Compile LaTeX document
        uses: xu-cheng/latex-action@v4
        with:
          root_file: qpdk.tex
          continue_on_error: true
          latexmk_use_xelatex: true
      - name: Copy PDF to HTML artifact directory
        run: |
          mkdir -p docs/_build/html
          cp docs/_build/latex/qpdk.pdf docs/_build/html/qpdk.pdf
      - uses: actions/upload-pages-artifact@v3
        with:
          path: "./docs/_build/html/"
  deploy-docs:
    name: Deploy to GitHub Pages
    needs: [build-docs, build-docs-pdf]
    if: ${{ github.ref == 'refs/heads/main' && github.event_name == 'push' }}
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
