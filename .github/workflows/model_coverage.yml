name: Model Coverage
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  model-coverage:
    uses: doplaydo/pdk-ci-workflow/.github/workflows/model_coverage.yml@ae9c40561a365ac980256651b03a34c604eaa904 # main
    secrets: inherit
