name: Test Coverage
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
permissions: {}
jobs:
  coverage:
    permissions:
      contents: write
      pull-requests: write
    uses: doplaydo/pdk-ci-workflow/.github/workflows/test_coverage.yml@ae9c40561a365ac980256651b03a34c604eaa904 # main
    secrets: inherit # zizmor: ignore[secrets-inherit]
