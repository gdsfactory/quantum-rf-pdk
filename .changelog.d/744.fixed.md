Analytical capacitor, inductor, and CPW coupling models now eagerly validate geometry (positive and finite values,
integral finger count of at least 2) and raise `ValueError` on invalid inputs. The registered SAX models call the jitted
cores directly, so `jax.jit`, `jax.grad`, and `jax.vmap` over model geometry parameters keep working.
